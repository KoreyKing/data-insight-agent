# 本地开发/贡献者：本地构建镜像（覆盖 docker-compose.yml 的 image）。
# 用法：docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: data-insight-agent:dev
